# Scheduler Service (monorepo)

This repo contains the scheduler microservice stack:
- apps/api-service (Fastify + TypeScript)
- apps/worker-service (TypeScript worker)
- apps/web (Next.js frontend)
- packages/db (Prisma schemas)
- packages/sdk (SDK to publish)


